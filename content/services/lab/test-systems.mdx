---
title: Test systems
description: Experimental and development workloads.
---

Last synced with live infrastructure: **September 2026**.

Not everything on the cluster is production, and this page is the proof. Development environments, agent sandboxes, and parked machines live here under one rule: they can change or vanish without notice, and nobody's evening is ruined when they do. Stopped guests stay listed anyway, because a powered-off VM still occupies disk, an ID, and someone's vague intention — calling them out is what turns good intentions into decommissioning decisions.

## Development and agent hosts

| Kind | ID | Name | Host | Status | Notes |
|------|----|------|------|--------|-------|
| LXC | `119` | `code` | `pve3` | Running | Dev environment, `code.cleeryy.cloud` behind Authentik |
| LXC | `125` | `hermesagent` | `pve3` | Running | Lab agent (`10.1.20.41` tag) |
| LXC | `110` | `discopanel` | `pve3` | Running | Game panel, `mc-dashboard.cleeryy.cloud` |
| VM | `10001` | `mint` | `pve3` | **Stopped** | Lab desktop |

The old EDR test containers (`331`–`333`) have been deleted.

## Placement

- All lab and test workloads live on `pve3`, distinct from the production stacks.

## Operational notes

- Stopped systems are documented because their existence is part of the architecture. They are decision items, not active workloads — see the [migration plan](/architecture/migration-plan).
- These systems are **not** monitored as production. Their state can change without notice.

## Pending

- Functional description and owner for `discopanel` and `hermesagent`.
- Decommissioning decisions for stopped systems — see the [migration plan](/architecture/migration-plan).
