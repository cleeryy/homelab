---
title: Pre-production
description: Pre-production lab containers on `pve3`.
---

## Components

| Kind | ID | Name | Host | Status |
|------|----|------|------|--------|
| LXC | `320` | `preprod-lockbits` | `pve3` | Running |
| LXC | `310` | `prod-lockbits` | `pve3` | Running |

`prod-lockbits` is documented alongside `preprod-lockbits` because they are part of the same lab namespace.

## Purpose

These containers are part of a `lockbits` lab namespace. The specific functional purpose is **not captured here**.

## Placement

- Both run on `pve3` under the lab namespace.

## Operational notes

- These are lab workloads. They are not part of the production data path and should not be relied on by user-facing services.

## Pending

- Functional description of each `lockbits` container.
- Network reachability and exposure model.
- Backup posture (likely none — lab workloads are typically ephemeral).
