---
title: Networks
description: Sites, bridges, VLANs, segments, and host addresses.
---

Last synced with live infrastructure: **September 2026**.

## Sites

| ID | Name | Edge | Notes |
|----|------|------|-------|
| `1` | mamie | OPNsense VM `501` (on `pve3`) | Server site, full Proxmox cluster |
| `2` | home | Fortigate 81E | Present but not configured; RPi services sit on the ISP CPE |

Routed networks follow `10.<site>.<vlan>.0/24`; the gateway is always `.1`.

## OPNsense interfaces (site 1)

| Role | OPNsense iface | Underlying | Address | Notes |
|------|----------------|-----------|---------|-------|
| WAN | `wan` | `vtnet0` | `192.168.1.250/24` (gw `192.168.1.1`) | Behind ISP CPE (double NAT) |
| LAN | `lan` | `vtnet1` | `10.1.1.1/24` | Untagged on the trunk |
| Admin | `opt1` | `vlan0.10` | `10.1.10.1/24` | VLAN 10 |
| Production | `opt2` | `vlan0.20` | `10.1.20.1/24` | VLAN 20 |
| Preproduction | `opt3` | `vlan0.25` | `10.1.25.1/24` | VLAN 25 |
| IoT | `opt4` | `vlan0.30` | `10.1.30.1/24` | VLAN 30 |
| Guest | `opt5` | `vlan0.40` | `10.1.40.1/24` | VLAN 40 |
| Management | `opt6` | `vlan0.50` | `10.1.50.1/24` | VLAN 50 |
| Home | `opt7` | `vlan0.60` | `10.1.60.1/24` | VLAN 60 |
| VPN (WireGuard) | `opt8` | `wg0` | `10.1.90.1/24` | Road-warrior server |
| AirVPN | `opt9` | `wg1` | `10.130.184.82/32` | Privacy egress client |
| NetBird | `opt10` | `wt0` | `100.121.135.95/16` | Mesh VPN |

## DHCP ranges (site 1, served by dnsmasq)

| Interface | Range | Pushed gateway |
|-----------|-------|----------------|
| Admin (`opt1`) | `10.1.10.100`–`10.1.10.199` | `10.1.10.1` |
| Guest (`opt5`) | `10.1.40.10`–`10.1.40.199` | `10.1.40.1` |
| Home (`opt7`) | `10.1.60.10`–`10.1.60.199` | `10.1.60.1` |

IoT (`opt4`) is bound by dnsmasq but no DHCP range is defined; the other VLANs do not run DHCP from OPNsense.

## WireGuard road-warrior peers (site 1)

| Peer | Tunnel IP |
|------|-----------|
| `m4-air` | `10.1.90.3/32` |
| `iphone15` | `10.1.90.2/32` |
| `windaube` | `10.1.90.4/32` |

Listen `udp/51920` on WAN, tunnel `10.1.90.0/24`, peer DNS pushed `94.140.14.14`, `94.140.15.15`. All peers showed offline at the last observation.

## AirVPN client (site 1)

Interface `wg1`, address `10.130.184.82/32` (+ IPv6 ULA), peer `AirVPN-NL3-peer` online with 15 s keepalive. Gateway `AIRVPN_GW` (`10.128.0.1`) online. The `INTERNAL` group policy-routes outbound traffic through it with a kill-switch beneath.

## NetBird mesh (site 1)

Interface `wt0`, address `100.121.135.95`. Seven peers online at the last observation (relay + direct, including production hosts and `pve1`).

## Key guest addresses (confirmed bindings)

| Guest | Address | How confirmed |
|-------|---------|---------------|
| Caddy (`2010`) | `10.1.20.10` | OPNsense NAT target for 80/443 |
| Warpgate (`222`) | `10.1.20.22` | OPNsense NAT target for SSH (`:2222`) |
| Palworld (`4001`) | `10.1.20.143` | OPNsense NAT target (UDP 8211/8212/27015) |
| PBS (`578`) | `10.1.50.78` | PVE `pbs` storage + `:8007` |
| Technitium (`553`) | `10.1.50.53` | Unbound override for `internal.cleeryy.cloud` |
| Dokploy host (`obiwan`, `205`) | `10.1.20.5` | Internal DNS + Caddy upstream |
| TrueNAS (`127`) | `192.168.1.74` | PVE `truenas` NFS storage |
| NAS | `192.168.1.16` | PVE `nas` NFS storage |
| `projet-paul` (`257`) | `10.1.25.7` | Preproduction tag |

Other LXC addresses follow the Proxmox tag convention and are indicative until confirmed by a binding above, a DNS record, or DHCP.

## Bridges (Proxmox, site 1)

| Bridge | Node | Address on host | Status |
|--------|------|-----------------|--------|
| `vmbr0` | `pve1` | `192.168.1.101/24` | Up |
| `vmbr0` | `pve3` | `192.168.1.103/24` | Up |

`vmbr1` and `vmbr2` no longer exist on `pve3`.

## Legacy segments (site 1)

Proxmox management and the legacy Docker VMs run on the flat network, not on the VLAN plan above.

| Segment | CIDR | Where it terminates |
|---------|------|---------------------|
| Management LAN | `192.168.1.0/24` | `vmbr0` on both nodes |

## Docker host addresses (site 1, legacy network)

| Beszel label | IP | Hostname (Beszel) | Proxmox VM | Node |
|--------------|----|--------------------|------------|------|
| `Beszel` | `192.168.1.25` | `beszel` | `202` `ubuntu-beszel` | `pve1` |
| `Jelly` | `192.168.1.31` | `jelly` | `300` `ubuntu-jelly` | `pve3` |
| `n8n` | `192.168.1.13` | `ntfy` | `204` `ubuntu-n8n` | `pve1` |
| `Nextcloud` | `192.168.1.27` | `nextcloud` | `200` `ubuntu-nextcloud` | `pve1` |
| `QBittorrent` | `192.168.1.28` | `qbittorrent` | `302` `ubuntu-qbittorrent` | `pve3` |
| `Servarr` | `192.168.1.26` | `servarr` | `301` `ubuntu-servarr` | `pve3` |
| `web-apps` | `192.168.1.23` | `ntfy` | `102` `ubuntu-small-webapps` | `pve1` |

Two hosts (`n8n` and `web-apps`) report the hostname `ntfy`; the cause is not documented.

## Site 2 (home)

- Fortigate 81E present, not configured.
- Raspberry Pi running Home Assistant, AdGuard Home, and iSponsorBlock. Sits on the ISP router; address depends on the CPE's DHCP and is not pinned here.

## Known gaps

Everything above is logical topology; the physical layer is still word of mouth. Switch port mapping, the inter-node link, and a cable-level diagram would complete site 1, while site 2 needs its Fortigate base config and any future VLAN plan written down before hardware gets touched.
