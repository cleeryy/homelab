---
title: Nodes
description: Physical Proxmox nodes.
---

## Active nodes

| Node | Role | Proxmox VE | Management | Gateway |
|------|------|-----------|------------|---------|
| `pve1` | Infrastructure and management | 9.x | `192.168.1.101/24` on `vmbr0` | `192.168.1.1` |
| `pve3` | Primary compute and production | 9.x | `192.168.1.103/24` on `vmbr0` | `192.168.1.1` |

### `pve1`

- Bridges: `vmbr0` (management LAN).
- Storage attached: `local`, `local-lvm`, `pve1-storage` (ZFS), `nas` (shared NFS).
- Hosts ingress, code/database/wiki stack, monitoring, application VMs running Docker stacks.
- Beszel reports ~83 systemd services monitored, no failed services at the last observation.
- Memory pressure reported higher than `pve3`.

### `pve3`

- Bridges: `vmbr0` (management LAN), `vmbr1` (`10.0.0.124`, internal/routed), `vmbr2` (`192.168.1.112`, **down**).
- Storage attached: `local`, `local-lvm` (large LVM-thin pool), `nas` (shared NFS).
- Additional physical disks passed through to the TrueNAS VM (`127`).
- No active ZFS pool on the host.
- Beszel reports ~82 systemd services monitored, no failed services at the last observation.

## Inactive

| Node | Status | Notes |
|------|--------|-------|
| `pve2` | Inactive | Not part of the active design. `pve2-storage` entry survives in `/etc/pve/storage.cfg`. |

## Pending capture

- CPU model, core count, and RAM size per node.
- Disk slot map per node, including which physical disks are passed to TrueNAS.
- BIOS/firmware levels.
